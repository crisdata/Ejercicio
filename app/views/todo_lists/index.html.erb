<p id="notice"><%= notice %></p>

<h1>Agenda</h1>

<table>
  <thead>
    <tr>
      <th>Titulo</th>
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @todo_lists.each do |todo_list| %>
      <tr>
        <td><%= todo_list.titulo %></td>
        <td><%= todo_list.descripcion %></td>
        <td><%= link_to 'Agregar', todo_list %></td>
        <td><%= link_to 'Editar', edit_todo_list_path(todo_list) %></td>
        <td><%= link_to 'Borrar', todo_list, method: :delete, data: { confirm: 'Estás seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva Lista', new_todo_list_path %>
